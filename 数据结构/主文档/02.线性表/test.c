#include <stdio.h>

typedef struct Node{
    int data;
    struct Node *next;
} Node,linklist ;

int main()
{
    int n,i,e;
    scanf("%d%d%d",&n,&i,&e);
    Node *L[n];
    
    return 0;
}
void insert_data(Node *L)
{

}